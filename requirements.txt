# Необязательные зависимости для расширенного извлечения метаданных.
# Программа работает и без них (базовые метаданные доступны всегда),
# но с ними поддерживается больше типов файлов.

Pillow>=10.0        # EXIF и свойства изображений (JPEG, PNG, TIFF, ...)
mutagen>=1.47       # теги аудио (MP3, FLAC, OGG, M4A, ...)
pypdf>=4.0          # метаданные PDF-документов
tkinterdnd2>=0.4    # перетаскивание файлов в окно (drag-and-drop)
